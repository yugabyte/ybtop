[project]
name = "ybtop"
version = "0.1.12"
description = "Terminal observability for YugabyteDB (pg_stat_statements, ASH, tablet views)"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: System Administrators",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Database",
]
dependencies = [
  "psycopg[binary]>=3.2",
  "rich>=13.7",
]

[project.optional-dependencies]
# Latency-histogram multimodality detection for watch --snapshot-latency-analysis
# sidecars (viewer Latency modes tab). diptest adds the confirmatory Hartigan dip test;
# without it, shape-flagged rows are reported as "unconfirmed".
histogram = [
  "numpy>=1.21",
  "scipy>=1.7",
  "diptest>=0.5",
]

[project.scripts]
ybtop = "ybtop.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
ybtop = ["web/*"]
